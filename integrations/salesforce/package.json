{
  "name": "salesforce",
  "scripts": {
    "check:type": "tsc --noEmit",
    "build": "bp build",
    "deploy": "bp deploy",
    "dev": "nodemon --watch \"./src/*\" --ext \"ts\" --exec \"bp deploy -y\""
  },
  "private": true,
  "dependencies": {
    "jsforce": "^1.11.1",
    "querystring": "^0.2.1"
  }
}
