{
  "name": "@bp-templates/hubspot-hitl",
  "type": "module",
  "scripts": {
    "check:type": "tsc --noEmit",
    "version": "bp --version",
    "login": "bp login",
    "gen": "bp gen",
    "deploy": "bp deploy",
    "test": "vitest --run",
    "test:watch": "vitest",
    "test:unit": "vitest --run --config vitest.unit.config.ts",
    "test:e2e": "vitest --run --config vitest.e2e.config.ts"
  },
  "private": true,
  "bpDependencies": {
    "hitl": "interface:hitl@2.0.0"
  }
}
